; Application runtime tuning for FrankenPHP/Octane.
memory_limit=512M
max_execution_time=60
post_max_size=128M
upload_max_filesize=128M
max_file_uploads=50
expose_php=0
date.timezone=UTC

; Keep the realpath cache warm across requests.
zend.assertions=-1
